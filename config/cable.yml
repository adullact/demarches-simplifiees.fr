default: &default
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: tps_production

production:
  <<: *default
  adapter: redis

development:
  <<: *default
  adapter: async

test:
  adapter: test
